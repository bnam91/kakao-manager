# 개인 설정/자격증명은 절대 커밋 금지 (레포 밖 ~/.config/kakao_manager/config.json 사용 권장)
config.json
scripts/config.json
*.env
.env

# 타겟(방 묶음) 실데이터도 커밋 금지 — 템플릿(target.example.json)만 추적.
# 실 타겟은 레포 밖 ~/.config/kakao_manager/targets/ 에 저장.
targets/
*.target.json

# python 캐시
__pycache__/
*.pyc
